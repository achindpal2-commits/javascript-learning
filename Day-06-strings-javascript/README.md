# Day 06 - JavaScript Strings

Today I learned and practiced **Strings in JavaScript**.

## Topics Covered

* Creating Strings
* String Length
* Accessing Characters
* `charAt()`
* `toUpperCase()`
* `toLowerCase()`
* `includes()`
* `startsWith()`
* `endsWith()`
* `trim()`
* `trimStart()`
* `trimEnd()`
* `slice()`
* `substring()`
* `replace()`
* `replaceAll()`
* `split()`
* `concat()`
* `repeat()`
* `indexOf()`
* `lastIndexOf()`
* Template Literals
* String Comparison

## Practice

Practiced different string methods and basic string operations using JavaScript.

## Goal

To understand how strings work and become comfortable with common string methods used in JavaScript.
